
  <% @books.each do |book| %>
    <div class="booyah-box col-10 offset-1">
      <h1><%= link_to book.title, book_path(book) %></h1><br />
      <i><%= book.title %></i><br /><br />
      <p><%= book.author %></p><br />

    </div>

    <hr />

  <% end %>   